package com.aronid.weighttrackertft.ui.screens.questionnaire

/*
@Composable
fun UserQuestionnaireNavigation(
    navHostController: NavHostController,
    viewModel: UserQuestionnaireViewModel
) {
    NavHost(navController = navHostController, startDestination = "personalInformation") {
        composable("personalInformation") {
            PersonalInformationScreen(
                innerPadding = innerPadding,
                viewModel = viewModel,
                navController = navHostController
            )
        }
        composable("lifeStyle") {
            LifeStyleScreen(viewModel = viewModel, navController = navHostController)
        }
        composable("physicalData") {
            PhysicalDataScreen(viewModel = viewModel, navController = navHostController)
        }
    }
}
*/
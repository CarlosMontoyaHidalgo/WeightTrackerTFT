<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.WeightTrackerTFT" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Changed: Set the window background to transparent -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- Optional: Make the status bar transparent or match your app's theme -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Added: Ensure the window is drawn behind the status bar -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- Added: Ensure the window content does not overlap with the status bar -->
        <item name="android:fitsSystemWindows">false</item>
    </style>

</resources>